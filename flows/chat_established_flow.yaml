name: chat_established_flow
states:
    first:
        component: meya.text
        properties:
            text: "You are now talking to {{ flow.agent_name }}."
    return_state:
        component: meya.pause